---
import BaseLayout from '../../../layouts/BaseLayout.astro';
import { connectToDatabase } from '../../../lib/db';
import { User } from '../../../models/User';
import { Follow } from '../../../models/Follow';

const { username } = Astro.params;
await connectToDatabase();
const profileUser = await User.findOne({ usernameLower: username?.toLowerCase() }).lean();
if (!profileUser) return Astro.redirect('/404');

const follows = await Follow.find({ following: profileUser._id })
  .sort({ createdAt: -1 })
  .populate('follower', 'username name avatarUrl bio')
  .lean();
---

<BaseLayout title={`People following ${profileUser.name}`} noIndex>
  <div class="mx-auto max-w-lg px-5 py-12 sm:px-8">
    <a href={`/profile/${profileUser.username}`} class="text-sm text-[var(--color-ink-soft)] hover:text-[var(--color-ink)]">&larr; Back to profile</a>
    <h1 class="mt-3 font-serif text-2xl font-semibold text-[var(--color-ink)]">Followers</h1>
    {follows.length === 0 && <p class="mt-8 text-sm text-[var(--color-ink-soft)]">No followers yet.</p>}
    <ul class="mt-6 space-y-4">
      {follows.map((f: any) => (
        <li>
          <a href={`/profile/${f.follower.username}`} class="flex items-center gap-3">
            <span class="flex h-10 w-10 items-center justify-center overflow-hidden rounded-full bg-[var(--color-paper-raised)] text-sm font-medium">
              {f.follower.avatarUrl ? <img src={f.follower.avatarUrl} alt="" class="h-full w-full object-cover" /> : f.follower.name.charAt(0).toUpperCase()}
            </span>
            <span>
              <span class="block text-sm font-medium text-[var(--color-ink)]">{f.follower.name}</span>
              <span class="block text-xs text-[var(--color-ink-soft)]">@{f.follower.username}</span>
            </span>
          </a>
        </li>
      ))}
    </ul>
  </div>
</BaseLayout>
